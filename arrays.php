<?php
$myArray = []; // пустой массив (работает только на PHP 5.4 +)
$anotherEmptyArray = array(); // ещё один способ создать массив

var_dump($myArray);
var_dump($anotherEmptyArray);

$numberArray = [1, 2, 3, 4];